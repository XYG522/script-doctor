# Script Doctor 面试准备材料

> 生成日期：2026-09-15 ｜ 配套：ARCHITECTURE_SUMMARY.md（事实依据）、DECISIONS.md（决策依据）、EVALUATION_PLAN.md（评估口径）
> 使用说明：§1-§6 按面试场景取用；所有回答要点都附了证据位置（文件:行号），被追问时可以直接甩出处。**先读 §0——这比任何话术都重要。**

---

## 0. 诚实底线（面试红线：以下事必须主动说或如实答，不可含糊）

| # | 红线 | 如实说法 |
|---|---|---|
| 1 | 演示闭环指标（68→78、漏洞 2→0、采纳 3/3） | **是预计算的演示资产**（app.py:977-981 硬编码、demo_report 为静态 JSON），不是真实用户数据。真实对比逻辑在 compare.py:28，演示资产是为了保证面试演示零故障 |
| 2 | 效果评估（完成率/引用准确率等） | **还没跑**。EVALUATION_PLAN.md 是方案不是结果；所有基线/目标数值标注为假设。面试时严格区分「已验证的机制」（340 项断言）与「待测的效果」（12 任务 A/B） |
| 3 | 无真实用户 | MVP 目标是验证闭环可行性，目标用户（编剧/制片人/剧本策划）是**定位**不是**已有用户**；下一步计划就是 5-10 个真实试用 |
| 4 | 项目规模 | 4 天（2026-09-12 启动 → 09-15 三阶段完成）、约 12 个源文件。不吹规模，吹**决策密度**——每个设计点都能被追问三层 |
| 5 | 合规 | 剧本原文会发给 DeepSeek API；数据训练条款**未做合规审查**——如实说「这是商业化前必做项」，不要装作已经处理过 |

---

## 1. 30 秒版本（业务语言，无技术词）

> 要求：产品是什么、解决什么问题、核心结果（量化）。不出现 Agent/Workflow/LLM。

**建议稿**：

> 我做了一个帮编剧「体检剧本」的工具：编剧上传剧本，两分钟内拿到一份七维体检报告——角色分布、节奏、逻辑漏洞、结构、商业潜力等，每条问题都指明具体哪一场、并附可直接替换的改写片段。报告里任何一个观点都能继续追问，回答引用的每一句台词都经过逐字核对，对不上原文的话宁可不说；剧本里没有的内容，它会明说没有。单次分析成本约一块钱（¥0.4–2.4），另有一组免费的规则检查。项目已跑通完整演示闭环：示例剧本按建议改稿后，总分从 68 提升到 78、逻辑漏洞清零。

**量化点出处**：七维报告 + 改写片段（README 功能清单）；70–140 秒（README 运行说明）；¥0.4–2.4/次（README 成本表）；引用逐字核对（analyzer.py:241）；「无相关信息」明确拒答（doctor_agent.py:98-99）；68→78 闭环（demo_report v1/v2，**属红线 1，被追问时如实说**）。

---

## 2. 3 分钟版本（问题→方案→关键决策→评估结果→边界）

**问题**：编剧写完剧本，缺少客观、快、便宜的反馈。找人看：贵、慢、主观；用通用 AI 问：只会给笼统意见，不落到具体场次，还会编造台词。

**方案**：剧本体检报告。输入剧本 → 输出七维诊断 + 3 条可落笔建议 + 每条建议配可直接替换的改写片段；报告可追问（回答引用逐字可验）。目标用户：编剧、制片人、剧本策划；定位收敛为电影短片/单集剧本（≤2 万字）。

**关键决策（讲 2 个）**：
1. **分析用固定流程、追问才用动态查证**。分析任务结构是已知的（7 个固定维度），固定流程换来三个东西：单次成本精确到 ¥0.4–2.4 可报价、七个维度并行分析延迟压到串行的约七分之一、每个环节独立校验失败可定位（analyzer.py:658 硬编码流程 / :749 并行）。追问的问题不可预枚举，所以追问层让 AI 自己决定查什么，但限死在 6 步内、3 个只读工具（doctor_agent.py:23/44）。
2. **引用逐字硬校验——宁可错杀不可漏放**。AI 回答里每条台词引用都必须与原文逐字对上，对不上的直接剔除（analyzer.py:241）。代价是可能误删一两条改写过的引用，但漏放一条假引用 = 编剧对整个工具失去信任，这个账不值得赌。

**评估结果（诚实分层）**：机制层已验证——8 个测试套件 340 项断言全绿（含「编造引用被剔除」的专项用例，tests/test_agent_chat.py:120-128）；效果层待测——12 任务 A/B 评估方案已设计好（Agent vs 单轮对照），计划下周跑。

**一个失败案例**：早期版本 AI 会自信地编造剧本里没有的台词当引用——测试里专门构造了这种输入，现在这类引用 100% 被拦截；但对不上原文的引用是「剔除」而不是「阻断回答」，保留答案、去掉假证据（tests/test_agent_chat.py 用例 2）。

**边界**：评分是参考意见不是行业标准；>2 万字自动转分块分析、跨块的长线逻辑检测有限（已知限制，README 如实声明）；PDF 扫描件需转文字；商业化与合规未做——这是 MVP 的诚实边界。

---

## 3. 混合架构讲述脚本（专答「你这是 Agent 还是 Workflow」）

> 面试官问「你这是 Agent 吗」，用下面这段。核心姿态：**承认 + 画线 + 讲理由 + 升华**。

**承认**：不全是。我的产品分两层，这个分层是刻意的。

**画线**：分析管道是**确定性流程**——解析、七个维度、评分、改写，四轮十次调用，顺序写死在代码里（analyzer.py:658），每一步输入输出都有固定格式。对话层是**Agent**——用户追问时，AI 可以调用 3 个工具（搜原文、读场次、看报告章节）多轮查证后再回答，最多 6 步，查证过程实时展示（doctor_agent.py:293、app.py:885）。

**讲理由（为什么这样分工）**：
- 分析要**成本可控**：固定流程 = 单次成本精确可预估（上传前就能看到三档价格），换成 Agent 分析，成本上不封顶、没法给用户报价。
- 分析要**质量可控**：每个环节独立校验，哪个维度出错就降级哪个、报告照出（analyzer.py:268）；Agent 的探索路径不可枚举，校验点都没法埋。
- 追问要**动态查证**：用户的问题不可预枚举——「这句台词在哪场」「这个漏洞成立吗」，必须让 AI 自己决定查什么；单轮回答凭记忆必编造，查证 + 逐字校验才立得住。

**升华**：这中间体现的是 AI PM 的判断——不是「用不用 Agent」的选边站，而是**按环节的风险与成本结构选架构**：流程已知的环节买确定性，问题开放的环节买灵活性。两种模式的代价我都能说清（Agent 每问 2–6 次调用、单轮会编台词），这个边界是画出来的，不是默认的。

---

## 4. 面试官最可能追问的 20 个问题

### 架构类（5）

**Q1 ★「这不就是个 Prompt 链吗？」**
回答要点：是，但只对了一半——Prompt 链只是骨架，我的工作不在串调用，在**骨架外的三层**：①每轮输出都过格式校验 + 自动修复重试，失败降级不崩（`_run_module` analyzer.py:268）；②每条引用逐字硬校验（analyzer.py:241）；③一组完全不用 AI 的规则层——出场场次是规则统计的（build_stats analyzer.py:219）、伏笔回收是规则判的（analyzer.py:495）、对白占比/场长是本地算的（hardcheck.py）。随便串 prompt 不需要 8 个测试套件 340 项断言来兜。

**Q2 ★「为什么分析不用 Agent？」**
回答要点：分析任务结构已知（固定 7 维度 × 全文），没有「不可预枚举的下一步」，动态决策是纯浪费。固定流程换来三样 Agent 给不了的：成本可报价（¥0.4–2.4/次三档预估）、七模块并行（延迟 ≈ 串行 1/7，analyzer.py:749）、每轮独立校验可定位降级。而且「条件深挖」（发现漏洞后自动取证）的价值我认——已列入第二阶段路线图，等真实数据证明有模块对部分剧本是浪费时再上（DECISIONS.md 决策 1 反证条件）。

**Q3 「4 轮调用为什么要拆开，一次大 prompt 不行吗？」**
回答要点：拆开是为了三个「不让模型干的活」：R1 解析出角色名单后，出场场次用**规则统计**不数 LLM（analyzer.py:732）；R2 七模块相互独立才能并行；R4 改写只喂「建议 + 落点场次原文」而不是全文，成本降一个量级（analyzer.py:794-818）。每轮还各自有校验注入点——拆开才有地方埋防幻觉的桩。

**Q4 「为什么工具只有 3 个，还全部只读？」**
回答要点：追问场景的问题类型就三类：查台词（search_script）、看场次细节（get_scene）、问报告结论（read_report_section）。只读是防幻觉体系的承重墙：工具返回全部由本地规则检索生成、不经过 AI（doctor_agent.py:117-191），AI 收到的每个字都真实存在；一旦给写工具，AI 可以改完剧本再引用自己改的内容，逐字校验形同虚设。报告章节读取还做了枚举白名单（doctor_agent.py:39-42）。

**Q5 「上下文怎么控制？长剧本怎么办？」**
回答要点：产品定位先收敛——短片/单集 ≈ ≤2 万字，全文直接进上下文，所以**不需要 RAG**（这是有依据的架构判断，不是没做）。>2 万字自动转分块：场头概览 → 每 12 场一块、块间重叠 1 场保边界连续性 → 汇总合并（analyzer.py:31-33、:529）。对话层的细节靠工具按需取原文，引用校验始终对全文做（doctor_agent.py:368-370）。

### 决策类（5）

**Q6 ★「Agent 比单轮贵 3 倍但只提升 5%，你怎么决策？」**
回答要点：先把「3 倍、5%」换成可验证的口径——我的评估方案就是为这道题设计的：同一批 12 个任务跑 Agent 和单轮对照，判定阈值是**完成率优势 ≥10pp 且引用准确率优势 ≥20pp 才值得**（EVALUATION_PLAN §4.3）。预期分化：原文核实类（台词在哪场）Agent 碾压，纯报告摘要类（报告说了什么）Agent 无增益——所以正确的解不是二选一，是**问题分类路由**：核实类走 Agent、摘要类走单轮，成本降、质量不降。如果数据跑出来 Agent 全输，我改回单轮——决策被反证条件钉住了，不是信仰。

**Q7 「6 步上限怎么定的？」**
回答要点：结构是必要的（成本封顶 + 保证必有回答：第 6 步即使还在查也强制收尾，doctor_agent.py:339-342），数字是拍的（典型查证 3 步 × 2 倍余量）——我把它列为自己最不确定的三个决策之一，已写进校准计划：用无上限版本采集真实步数分布后回填（DECISIONS.md 决策 4）。

**Q8 「宁可错杀引用，会不会误伤体验？」**
回答要点：会，这是明算过的账：漏放一条假引用 = 编剧发现 AI 编台词 = 信任崩塌，一次就死；误杀一条引用 = 少一条证据，答案还在。所以设计成「剔除引用、保留答案 + 警告」（tests/test_agent_chat.py:120-128 专门测这个）。误杀的代价我也量化了——评估指标里加了「引用误杀率」，目标 <20%，超标就上宽松匹配二档。

**Q9 「历史报告为什么不能继续追问？」**
回答要点：因为历史库只存了报告的完整 JSON + 剧本的 200 字摘录，没存全文（history.py:17）——而追问需要全文做工具检索和引用校验。这是存储成本 vs 对话能力的取舍；要改就是 save_report 加一个全文字段，改动路径清楚。

**Q10 「为什么选 DeepSeek 而不是 Claude/GPT？」**
回答要点：三个理由：成本（官方价输入 $0.66/M、输出 $1.98/M，同量级国际大厂贵约一个数量级——单次分析 ¥0.4–2.4 的成本故事依赖这个）；能力（json 结构化输出和工具调用都实测可用，中文参数无损）；兼容（OpenAI 兼容接口，llm.py 适配层换供应商不动业务代码，340 项断言兜底回归）。代价：供应商风险，如实承认。

### 评估类（4）

**Q11 「你的评估是怎么做的？」**
回答要点：两层。机制层：8 个测试套件、340 项断言全绿，全部用假客户端打桩、不发网络、可离线复现（tests/test_agent_chat.py 头部注释）。效果层：12 任务 × 3 次重复 × Agent/单轮两组对照的 A/B 方案已设计好（含任务 JSON、判定标准、badcase 预测和归因方法），待执行——如实说这是方案不是结果。

**Q12 「12 个任务是不是太少？结论能信吗？」**
回答要点：少，所以定位是 v0——目标是跑通口径、产出校准数据，不是发表结论。设计上做了三个补救：每个任务跑 3 次取中位数对抗随机性；「报告与原文冲突」类任务用**种植法**（人为在报告里埋 2 处错误）保证冲突可复现，不靠模型偶然犯错；盲评。v1 再扩长剧本任务集和 LLM 双盲抽查。

**Q13 「引用准确率 100% 是不是作弊？」**
回答要点：单报 100% 就是作弊——因为展示前的引用已经被硬校验滤过一遍，这是构造性保证，不是模型变好了。所以必须连「引用误杀率」（被误删的正确引用比例）一起报，两个数放在一起才是诚实口径。评估模板里就是这么设计的（EVALUATION_PLAN §3）。

**Q14 「完成率 85% 就是产品价值吗？」**
回答要点：不是，完成率只是「答对了」，业务价值是「编剧改稿变快」。代理链：报告建议被采纳率 → 改稿迭代次数 → 时间成本。MVP 阶段先证明最底层的信任底线（引用真实可验、没有就明说没有）——没有这条，上面的指标全是沙滩上的楼。

### 边界与风险类（3）

**Q15 ★「如果用户上传 10 万字剧本会怎样？」**
回答要点：分两层答。机制上：>2 万自动分块，10 万字 ≈ 40 块，块间重叠 1 场，逐块分析后合并（analyzer.py:31-33）；能跑，但成本随字数线性涨，且**跨块的长线逻辑检测能力有限**——一个横跨 30 场的伏笔可能断掉。产品上：这是定位边界——我把产品收敛到「电影短片/电视剧单集」（≤2 万字），长片全本明确不做，README 里如实声明。被追问「那长片用户怎么办」→ 那是另一个产品，RAG/更长上下文是它的架构，不是给当前 MVP 加补丁。

**Q16 「模型说错话你兜得住吗？」**
回答要点：兜得住「结构」和「引用」，兜不住「判断」。结构：输出格式校验 + 修复重试 ≤2 → 降级空模块，报告照出（analyzer.py:268）。引用：逐字硬校验，假的必删。数字：出场/伏笔/对白占比全部规则计算，0 幻觉风险。判断：AI 对节奏、情感的主观评价错了，我的兜底是置信度锚点（0.3/0.5/0.7/0.9 分档提示）和「评分是参考意见非行业标准」的明示。兜不住的部分不装兜得住——这是诚实边界。

**Q17 「剧本是用户的私密稿件，安全合规怎么考虑？」**
回答要点：现状三件事实：密码 PBKDF2 加盐哈希存储（auth.py:39-43）、数据在本地 SQLite（users.db）、剧本原文需发送给 DeepSeek API 才能分析。如实承认：**数据训练条款未做合规审查、未做任何认证**——这是 MVP 的明确边界，商业化前必做项清单里排第一位。主动说这一点，比被问到再承认加分。

### 产品价值类（3）

**Q18 ★「这个产品你最大的贡献是什么？」**
回答要点（三选一，挑一个讲透）：①**边界判断**——把「该确定性还是该动态」按环节拆开决策，而不是选边站（管道固定流程 / 对话动态查证，各有代价且都能说清）；②**防幻觉体系**——逐字引用校验 + 规则层替代模型数数字 + 置信度锚点，四层叠加，每一层都有测试用例钉住；③**把成本变成产品功能**——上传前实时三档预估、对话区先预估后按实际累计、全不勾选 = 0 成本免费报告（analyzer.py:695），AI 产品的定价故事从第一天就讲得清。

**Q19 「凭什么用户用你，不用通用 AI 直接问？」**
回答要点：三个差异点：通用 AI 给笼统意见，我给到**具体场次 + 可替换的改写片段**（第 4 轮只喂落点场次原文生成）；通用 AI 会编台词，我的引用**逐字可验**、没有就明说没有；通用 AI 每次回答成本不可见，我的成本**消费前预估、消费后对账**。人工替代方案（找编辑看稿）的痛点：贵、慢、主观——这正是产品定位的空间。

**Q20 「MVP 下一步做什么？」**
回答要点：顺序是数据驱动的：①跑完 12 任务 A/B 评估，校准三个拍出来的数（步数上限、成本乘数、2 万字阈值）；②按评估结果做问题分类路由（核实类走 Agent、摘要类走单轮）；③5-10 个真实编剧试用，收集剧本长度分布和追问类型分布；④对话持久化 + 合规审查。每一步都有前一层的产出做输入，不是拍脑袋排期。

---

## 5. 三个主动展示的亮点

### 亮点 1：防幻觉四层体系（引用逐字校验是承重墙）

- **亮点是什么**：AI 说的每句引用台词都经过逐字硬校验，对不上原文的宁可剔除；出场场次、伏笔回收、对白占比这些数字全部规则计算，不让模型数。
- **支撑证据**：`_filter_quotes` analyzer.py:241；build_stats analyzer.py:219；_validate_structure analyzer.py:495；hardcheck.py（0 LLM 成本）；专项测试「未命中全文的引用被剔除」tests/test_agent_chat.py:120-128；AGENT_SYSTEM_ADDON 规则 2「找不到就明说没有，confidence 降 0.3」doctor_agent.py:98-99。
- **怎么自然引出**：演示报告页时，指着一处「引用原文」说：**「这里的每一条引用都是逐字核对过的——模型曾经会自信地编台词，现在编不出来的。我给您看一个反面测试用例。」** 然后切测试文件。

### 亮点 2：Workflow / Agent 混合架构的边界判断

- **亮点是什么**：不选边站——分析环节买确定性（成本可报价、可并行、可校验），追问环节买灵活性（动态查证、引用可验），两种模式的代价都量化了。
- **支撑证据**：analyzer.py:658（硬编码流程）vs doctor_agent.py:293（工具循环）；成本对比：分析 ¥0.4–2.4 精确预估 vs 追问每问 2–6 次调用封顶 6 步；DECISIONS.md 决策 1/2 的完整论证与反证条件。
- **怎么自然引出**：面试官问「你这是 Agent 吗」→ 用 §3 脚本接住，顺势说 **「这个分层是我最得意的一个判断，我可以把两边各自的代价都讲出来」**。

### 亮点 3：成本透明做成产品功能

- **亮点是什么**：AI 产品的定价故事从上传页就开始讲——分析前三档实时预估（缓存命中/闲时/高峰），追问区先预估后按实际累计，全部模块不勾选 = 0 成本纯规则报告。
- **支撑证据**：estimate_run_cost analyzer.py:65（本地计算、无成本）；三档价格 README 成本表；对话区「已调用 N 次」累计 app.py:907-911；0 模块分支 analyzer.py:695-721。
- **怎么自然引出**：演示上传页时点掉模块勾选，说：**「AI 产品的 PM 必须对每一分钱有数——我把成本预估做成产品功能，连『免费』都是一个明确定义的档位。」**

---

## 6. 如果只讲一件事（200 字讲述稿）

> 选题：防幻觉 / 引用硬校验。理由：最具体、最可验证、最能体现「PM 定义问题 + 设计约束 + 量化取舍」的完整链路。

> 我做的最得意的一件事，是给 AI 加了一条「不许编」的硬约束。编剧最怕 AI 瞎说，尤其是引用台词——模型会非常自信地编一句剧本里没有的话。我的解法是：AI 每次引用台词，系统先逐字核对原文，对不上的直接剔除——宁可少引用，不可假引用。接着，出场次数、伏笔回收这些数字全部改成规则计算，不让模型数。再加置信度分档，用户知道哪些结论该打问号。结果是：用户看到的每条引用都真实可查。这个设计的代价我也算过——可能误删一两条被改写过的引用，所以我把它做成了可测量的指标，让「宁可错杀」这个取舍有数据兜底，而不是一句口号。

---

## 7. 面试官可能的负面评价及我的回应

| 负面评价 | 回应要点 |
|---|---|
| 「这不就是调 API 套模板吗？」 | 套模板不需要：三层校验（格式修复/引用逐字/规则层）、成本模型、降级链路、340 项断言。价值不在「调用」在「把不可信的输出变成可信的产品」，这恰是 AI PM 的活 |
| 「没有真实用户，凭什么说有价值？」 | 承认。MVP 目标是验证闭环可行性；目标用户是定位不是已有用户。下一步计划就是 5-10 个真实试用——把「没有数据」变成「数据计划已排好」 |
| 「评估都没跑，效果数字是不是吹的？」 | 文档里所有效果数值都标了「假设/待测」，面试口径严格区分「已验证的机制」（测试全绿）与「待测的效果」（A/B 方案）。诚实分层本身是加分项 |
| 「演示的 68→78 是硬编码的吧？」 | 是，主动说（红线 1）。演示资产保证演示稳定；真实对比逻辑在 compare.py:28。主动坦白反而显得严谨 |
| 「一次一块钱，商业模式呢？」 | MVP 没做商业化，但成本透明是定价的地基：知道单次成本 ¥0.4–2.4，才知道按次收费/订阅的定价空间；未来可加人工复核增值服务 |
| 「技术深度不够（没自训模型、没 RAG）」 | AI PM 的深度在问题定义、约束设计、评估体系，不在调参。RAG 是「有依据地不做」：定位 ≤2 万字全文进上下文，不需要检索增强——需要的话 10 万字场景会重新架构，那是另一个产品 |
| 「测试都是自己写的，自证没有说服力」 | 单元测试本来就是回归机制、不是效果证明——它的作用是「机制不坏」。效果要靠 A/B 评估（已设计、待跑）。两个东西我分得清 |
| 「4 天做的东西，深度有限吧？」 | 规模如实说；但每个设计点都能被追问三层（直接理由→约束分析→反证条件），展示的是决策密度而不是代码量。判断力不按工时计 |
| 「你是 PM 还是工程师？代码都是你写的吗？」 | 如实答。AI PM 岗位的加分项正是「能把需求落成代码、把方案落成测试」——评估方案、决策记录、架构文档三件套就是 PM 的工作方式，代码是实现手段 |
